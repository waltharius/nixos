# WiFi Secrets Template
# This is an example file - the actual secrets/wifi.yaml is encrypted with SOPS
#
# To create your encrypted WiFi secrets:
# 1. Copy this template:
#    cp secrets/wifi.yaml.example secrets/wifi.yaml
# 2. Edit with SOPS:
#    sops secrets/wifi.yaml
# 3. Replace placeholder values with your actual WiFi passwords
# 4. Save and exit - SOPS will encrypt automatically

# ==========================================
# WiFi Network Passwords
# ==========================================
# Format: wifi/network-name-psk: YourPasswordHere

# Home WiFi password
wifi/home-psk: REPLACE_WITH_YOUR_HOME_WIFI_PASSWORD

# Work WiFi password
wifi/work-psk: REPLACE_WITH_YOUR_WORK_WIFI_PASSWORD

# Parents WiFi password (optional)
wifi/parents-psk: REPLACE_WITH_YOUR_PARENTS_WIFI_PASSWORD

# ==========================================
# Additional Networks (Optional)
# ==========================================
# Add more networks as needed using the same format:
#
# wifi/vacation-home-psk: YourVacationHomePassword
# wifi/office2-psk: YourOffice2Password
# wifi/eduroam-password: YourEduroamPassword

# ==========================================
# Hidden Networks (Optional)
# ==========================================
# For hidden networks, you can also encrypt the SSID:
#
# wifi/hidden-ssid: SecretNetworkName
# wifi/hidden-psk: SecretNetworkPassword

# ==========================================
# IMPORTANT NOTES:
# ==========================================
# - SSIDs (network names) are NOT secret and are defined in modules/system/wifi.nix
# - Only passwords need to be encrypted here
# - After editing, run: sops updatekeys secrets/wifi.yaml
# - This file is encrypted at rest and safe to commit to git
# - Decrypted secrets only exist in /run/secrets/ (tmpfs)
